name: chained

on:
  workflow_run:
    workflows:
      - main
    types:
      - completed

jobs:
  echo-context:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ toJson(github.event.workflow_run) }}"
